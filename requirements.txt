# =============================================================================
# PennyMac Trading Desktop Agent - Python dependencies
# Tested against Python 3.11+ (developed on 3.13).
# Install with:  pip install -r requirements.txt
# =============================================================================

# --- MCP server (exposes tools to Cursor subagents) ---
mcp>=1.2.0

# --- Data / Snowflake ---
snowflake-connector-python[pandas]>=3.12.0
pandas>=2.2.0
pyarrow>=16.0.0          # required by snowflake fetch_pandas_all()

# --- Data / SQL Server (legacy, transitioning to Snowflake) ---
pyodbc>=5.1.0            # ODBC driver bridge for SQL Server
SQLAlchemy>=2.0.0        # connection/engine + named-param binding for pandas

# --- Excel modeling & VBA ---
xlwings>=0.31.0          # live workbooks + VBA macro execution (Windows/Mac, needs Excel)
openpyxl>=3.1.0          # headless read/write fallback

# --- Config & settings ---
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0
PyYAML>=6.0.0

# --- Dev / testing ---
pytest>=8.2.0
